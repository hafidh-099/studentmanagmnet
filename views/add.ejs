<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>add</title>
    <link rel="stylesheet" href="./styles/add.css" />
  </head>
  <body>
    <h2>Add Student</h2>
    <form method="POST" action="/add" novalidate>
      <input
        type="text"
        name="name"
        placeholder="Name"
        required
        class="<%=validateError.find(e=>e.path==='name') ? 'ivalid':'' %>"
      /><br />
      <input
        type="text"
        name="Vname"
        placeholder="varify Name"
        required
      /><br />
      <input
        type="email"
        name="email"
        placeholder="Email"
        required
        class="<%=validateError.find(e=>e.path==='email') ? 'ivalid':'' %>"
      /><br />
      <button type="submit">Add</button>
    </form>
  </body>
</html>
